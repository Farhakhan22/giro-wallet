export class SurbUserData {
    public userget: User[];
    }

export interface User {
    
    FirstName : string
   MiddleName: string
   LastName : string
   Mobile:number
   email : string
   password : string
   Referral: number
}